<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Normal permission: granted at install, no runtime prompt. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- T-019: the app is its own notification client (no FCM). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- T-020: local calendar reminders; exact only when OS-granted,
         inexact fallback otherwise (see ReminderScheduler). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- T-021: Phase 11 voice input — runtime permission, prompted on
         first mic tap (see MicButton). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- T-042: launch chronology for the volume-long-press app switch.
         Appop permission — granted via ADB (`appops set … GET_USAGE_STATS
         allow`) or Settings → Special app access → Usage access. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- T-042: Android 11+ package visibility — without this, getLaunchIntentForPackage()
         returns null for every app but ourselves (verified on-device: "no launch intent"
         for net.sourceforge.opencamera / org.koreader.launcher). MAIN/LAUNCHER query
         makes all home-screen-launchable apps visible. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".KompaktApplication"
        android:label="Kompakt"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sideloaded/de-Googled: specialUse FGS holds the SSE stream.
             (dataSync has a 6h OS cap on API 34+ — not viable for a
             persistent connection; Play policy doesn't apply to us.) -->
        <service
            android:name=".notifications.AlertStreamService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="persistent-sse-alert-stream" />
        </service>

        <!-- T-044 (D032): the sync-window service — the only thing that
             brings the embedded wg tunnel up (background windows from the
             15-min alarm + interactive windows while the app is used). -->
        <service
            android:name=".sync.SyncWindowService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="windowed-wireguard-sync" />
        </service>

        <!-- T-044: required declaration for the userspace wg backend
             (com.wireguard.android:tunnel GoBackend). System-bound. -->
        <service
            android:name="com.wireguard.android.backend.GoBackend$VpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false" />

        <!-- T-044: 15-min sync-window tick (exact alarm; inexact fallback
             inside SyncWindowScheduler). -->
        <receiver
            android:name=".sync.SyncWindowReceiver"
            android:exported="false" />

        <!-- T-044: alarms die at reboot — re-arm the window cadence. -->
        <receiver
            android:name=".sync.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- T-042: no-root recents access on inkOS — Volume Up long-press
             opens the SystemUI overview (GLOBAL_ACTION_RECENTS). -->
        <service
            android:name=".recents.RecentsKeyService"
            android:label="@string/recents_key_service_label"
            android:process=":keys"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/recents_key_service_config" />
        </service>

        <!-- T-043: system-notification read access (inkOS renders none).
             Listener binding is granted via ADB (`cmd notification
             allow_listener dev.magnor.kompakt/.notifications.NotificationListener`)
             or Settings → Special access → Notification access. -->
        <service
            android:name=".notifications.NotificationListener"
            android:label="Notification reader"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- T-020: AlarmManager reminder delivery (protocol §5). -->
        <receiver
            android:name=".notifications.ReminderReceiver"
            android:exported="false" />
    </application>

</manifest>
