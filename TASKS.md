# Tasks
<!-- synced from Obsidian Vault — edit here or in Obsidian -->

- [x] T-001 Phase 0 — Repository & tooling foundation
- [x] T-002 Phase 1 — App shell + E-Ink navigation (17 placeholder screens, mocked data)
- [x] T-003 Phase 2 — Domain model + repository interfaces (mock fakes)
- [x] T-004 Phase 3 — Server contract skeleton (capabilities + read endpoints)
- [x] T-005 Phase 4 — Device enrollment + security boundary
- [x] T-006 Phase 5 — Today + Organize vertical slice
- [x] T-007 Phase 6 — Capture pipeline (interpret → confirm → commit, offline queue)
- [x] T-008 Phase 9 — Connectivity watcher (auto-flush parked captures on reconnect, no restart)
- [x] T-009 Phase 7 — Chat client (send + readTimeout fix, E2E verified)
- [x] T-010 Phase 8 — Agents surface: V-052 contract port, backends/roles/runs UI, dispatch, steering, fakes + tests
- [x] T-011 Remote E2E — live agents smoke: LiveServerSmokeTest gains agents test (surface decode both backends, pi spawn_only honest caps, merged runs w/ prompt-derived titles, terminal events+result incl. salvage interpretation, unsupported steer, real opencode dispatch round-trip); AgentBackendInfo.name now optional (live /v1/agents keys backends by name — values carry only capability booleans); coordinator-side companion fix = V-053 (Warren v0.18 wire shapes). **PHONE E2E PASSED Aug 23** (post-merge): agent.write granted via admin approve; on-device dispatch "Reply with the single word READY and stop." → ses_fd049bdabffe9zPEjBy1uKSG1r (title "Readiness check") → glm-5.2 replied READY in ~3 s → run detail renders result + events + explicit transitions (Discuss in chat / Create task / Save note). Minor UX gap logged: role-screen Runs list doesn't refresh after dispatch (back+re-enter required); dispatched-session note not tappable.
- [x] T-012 Dispatch-UI polish — fix the two T-011 gaps: (1) role screen Runs list refreshes after dispatch via refresh-tick + flatMapLatest re-collection of the one-shot cold observe flow (same pattern as ChatThreadViewModel; run-detail screen gets the same treatment after send/steer/cancel/command so the status row updates without re-entering); (2) dead-end "Dispatched ses_…" text replaced by a tappable row (lastDispatched → onOpenRun). Regression tests via ColdAgentRepository (cold one-shot fake mirroring RemoteAgentRepository semantics — hot FakeAgentRepository hid the bug).
- [x] T-013 Chat/conversation UX overhaul (one pass, both screens) — shared ChatScaffold (Common.kt): top bar + LazyColumn transcript + fixed bottom composer, auto-jump to latest, no animation (e-ink). ChatThread: monochrome sender coding (user = right-shifted card semi-bold, assistant = full-width plain), per-message timestamps, tap-message actions (Edit / Revert to here / Regenerate — destructive, no branches), inline Jump-to-message index, editing banner + cancel, inline status/retry rows. AgentRunDetail: same scaffold — transcript-first (dialogue events full, process events dim one-liners), status collapsed to one summary card + expandable Details, commands behind "Run command" menu section, transitions compacted to Discuss/Task/Note row, composer = message + Send/Steer. Backend primitive: coordinator V-054 POST /v1/chats/{id}/truncate (server d535f60, 14 route tests); app composes truncate+send for revert/edit/regenerate. 5 new VM tests (ChatViewModelsTest, cold-fake truncate semantics).
- [x] T-013 fix: AppContainer default clock was frozen FakeData.NOW in prod → every server timestamp read "soon" (future); now Clock.System::now (tests inject frozen clock explicitly). timeOfDay() renders local tz, not UTC. Server V-055 auto-titles placeholder threads from first message (main 048283a).
- [x] T-014 Markdown rendering for LLM replies — new ui/Markdown.kt: dependency-free parser (blocks: paragraphs, #-headings, bullet/ordered/task lists w/ indent nesting, fenced code, >-quotes, rules; inline: **bold**, *italic*, ***both***, `code`, ~~strike~~, [label](url)) + MarkdownText composable rendering via TextMMD's AnnotatedString overload, monochrome-only styling (weight/family/decoration/size — D017). Deliberate omissions: `_emphasis_` (snake_case safety), nested inline styles, tables. Readability: lineHeight 21sp body, meta line spacing, heading sizes 19/17/15sp, code in bordered card @12sp monospace. Wired into chat message rows (user baseFontWeight=SemiBold), agent run dialogue events, jump-index previews (mdPreview strips markers); composer maxLines 4→6. 18 parser tests (MarkdownTest).
- [x] T-015 Compact single-row composer — send button moves from a full-width row below the field to a compact IconButton beside it (Icons.AutoMirrored.Filled.Send, in material-icons-core — no new dependency), bottom-aligned so it tracks the last line as the draft grows. Field label → placeholder (full inner height usable). Composer drops from ~120dp (field + 8 + button row) to ~64dp. Applied to both composers: chat thread (edit banner's ✕ now the sole cancel-edit affordance; redundant "Cancel edit" wide button removed) and agent run detail ("Message the session"; Steer, shown only when a backend advertises liveSteering, becomes a secondary outlined button below). Sending state = disabled icon + existing transcript placeholder row.
- [x] T-016 Inverted polarity theme ("dark mode") — e-ink has no battery argument; the value is night reading (inverted background absorbs frontlight instead of reflecting it). Polarity flip only: one mirrored ColorScheme derived from MMD's `eInkColorScheme` by inverting grayscale luminance per role (scrim stays black — scrims dim, never brighten). Lossless because the app has zero hardcoded color literals (verified). In-app setting Light/Inverted via Settings → Appearance (two ListRows, ✓ marks the active), default Light, file-backed ThemeStore (theme.txt, unknown values → Light). Explicitly NOT bound to system night mode (dusk auto-flip forces a full e-ink redraw mid-use; Mudita system UI inversion unverified). No system-bar styling (app ships no XML theme). Implementation notes: flip is an exact bitwise RGB mirror preserving each role's alpha (MMD's surfaceContainer tier is deliberately transparent — borders-only cards over the root Surface, which KompaktTheme now paints with scheme.background; previously the white background was the platform window, not the theme). Structural MMD facts pinned by mmdSchemeFactsTheInversionReliesOn.
- [x] T-017 Session resume + poll-while-running — AgentRunDetailViewModel observes resumed external sessions end-to-end and auto-settles a live turn without re-entering the screen. Poll architecture (the hard-won part): ticker re-raises the refresh tick every 5 s while BOTH (a) the shared run chain is subscribed — onStart/onCompletion pinned to the stateIn(WhileSubscribed) gate, so polling parks exactly when the screen leaves, grace-respecting — and (b) the observed run is active (QUEUED/RUNNING); settle → ticker parks, final cold fetch lands result+events once. Covers send-, dispatch-, and resume-initiated turns. Root cause found via jstack on a spinning Gradle worker: the pre-existing "send refreshes run state" test leaves the run RUNNING forever; with a self-re-scheduling delay loop, runTest's teardown advanceUntilIdle never idles → infinite 100 %-CPU spin, no XML, no timeout. Two designs rejected en route: manual job + @After reflection clear() (NoSuchMethodException — clear() not reflective-reachable in this lifecycle version; also too late — spin happens inside runTest before @After) and subscriptionCount-gated combine (deadlock: the gate's own collector keeps subscriptionCount ≥ 1 forever). The chain-gate design needs no test hygiene at all — cancelling the collector inside the test body parks the loop before teardown. 3 new virtual-time tests on the cold fake (poll-until-settle w/ evidence, settle-stops-polling, no-poll-when-idle). Coordinator companion = V-056 (project_ref ""→None). LiveServerSmokeTest gains resume leg (external session list → send → terminal events + result). LIVE SMOKE PASS (Aug 24, 139/139 incl. 4 live tests) surfaced three real defects, all fixed: (1) resume-leg race — send fired while turn one still ran; opencode answers busy with SessionBusyError→409, which HttpApi mis-mapped to RevisionConflictException ("revision 0"). Fix is two-sided: HttpApi now distinguishes revisioned 409s (current_revision present → conflict) from domain 409s (server detail surfaces honestly), and the composer is gated by a new canSend StateFlow (busy turn → disabled field, "Session running…" placeholder; +1 VM test, +1 contract test). Live test waits for turn-one settle first — same flow as the product. (2) sender coding was broken on the events transcript: both adapters emit kind="message" with the real sender in payload.role; ChatEventRow keyed on kind → every opencode row rendered as assistant. AgentEvent gains role, decode carries payload.role, row codes (role ?: kind). (3) Wire truth pinned: OpenCode's session view does not project the initial dispatch prompt — durable resume evidence = turn-1 assistant + resumed user + assistant reply (test asserts exactly that). Env lesson: the coordinator on :8650 must be started with WARREN_API_TOKEN (from docker inspect warren) + .env.local sourced, else warren silently vanishes from /v1/agents and the T-011 pi assertion fails; also KOMPACT_LIVE_* exports persist across Hermes terminal calls (session env snapshot), so verify.sh runs live legs whenever test sources changed.
- [x] T-018 Close the agent loop (V-057, Phase 10 slice): inbox + Today attention rows with `source_type=agent_run` deep-link to the run detail screen (Routes.run(sourceId) — coordinator alerts carry source_id = backend execution id) instead of the generic item detail; unread agent alerts render with outcome coding (failed = attention). Routing decision extracted testable (inbox target resolver); fakes gain agent_run-sourced inbox items. Server companion = V-057 (watcher + alerts + ntfy). Implemented: Routes.run(id) + Routes.fromInboxItem (agent_run → run screen, else generic item detail) wired in KompaktApp for both Today attention rows and Inbox; InboxViewModel.dismiss now real via remote inbox repo → POST /v1/inbox/{id}/read (was a stub); InboxViewModelTest added (VM→repo→state flow). verify.sh 145/145 + BUILD SUCCESSFUL. Server-side live E2E green Aug 24 (V-057 dispatch→alert→ntfy→read); on-device leg rides the next APK install (see T-016).
- [x] T-019 In-app alert notifications (server V-058) — the app becomes its own notification client (D008/D009: foreground in-app SSE transport, no FCM, no external push app). AlertTransport interface (D008 swappable) + pure SseParser + SseAlertTransport (OkHttp): Bearer via tokenProvider, readTimeout = 3× heartbeat, infinite reconnect w/ 1s→60s backoff (reset once a stream establishes), malformed SSE block → skipped inline (never kills the stream), clean EOF → reconnect (server's replay-on-connect makes a torn final block harmless). AlertStreamService: specialUse foreground service (typed startForeground only on API 34+ — unknown FGS type bits throw on 29–33) holds the socket; quiet persistent "listening" notification + per-alert notifications via AlertNotifications (stable notification IDs derived from alert id, dedupe, HIGH channel for failed runs); tap → route extra → MainActivity → Routes.run deep link (T-018 resolver) + auto mark-read. Lifecycle: KompaktApplication collects remoteActiveFlow → startForegroundService/stopService — stream lives exactly while remote mode is active. MainActivity rewritten: POST_NOTIFICATIONS runtime request (API 33+), launchRoute/onRouteConsumed into KompaktApp (LaunchedEffect nav). Tests: AlertNotificationsTest + AlertTransportTest (MockWebServer: blocks, timeout, malformed, EOF) — 160/160 green. Hard-won lessons: (1) kotlinx Flow emission invariant — withContext(IO) inside flow{} already under flowOn(IO) throws IllegalStateException, silently swallowed by the generic catch → presented as a SocketTimeout reconnect loop; root-caused via env-gated KOMPAKT_SSE_DEBUG probe (kept, zero-cost when unset). (2) Canonical OkHttp line loop = readUtf8Line() ?: break. **Server-side live E2E green Aug 24 (V-058: replay + live event ~6s after dispatch). On-device verify + APK install pending (user away; 3 unread alerts will exercise on-device replay on first connect).**
- [x] T-020 Phase 10 close-out — background fallback + local reminders (protocol §4.2 + §5), app-side only (no server changes). (1) AlertPoster: single notification post-point shared by all three delivery paths (SSE service, fallback worker, reminder receiver); channels (connection/alerts/alerts_high/reminders), notification-permission no-op guard, tap → route extra deep link. (2) AlertSeenStore: persisted bounded LRU (256) of alert ids, atomic tmp+rename writes — dedupe now survives process death, so worker-after-reboot doesn't re-notify unread items the live path already showed; shared by service + worker (whichever sees an alert first wins). (3) AlertSyncWorker: 30-min periodic WorkManager (KEEP, CONNECTED constraint, exp backoff, ≤3 retries; CancellationException rethrown — never map WM stop to retry); fetches /v1/inbox (server returns unread-only — verified live: dismissed run absent), posts unseen via shared store; core extracted context-free (fetchAndPostUnseen) so it unit-tests against MockWebServer. (4) Local reminders: ReminderPlanner (pure: future events in 24h horizon, alarm at start−10min, events already inside their lead window SKIPPED not fired — replan anti-spam) + ReminderScheduler (AlarmManager; exact when canScheduleExactAlarms() on S+, else inexact setAndAllowWhileIdle — sideloaded apps don't get exact by default on 33+; full-replan semantics, stale alarms cancelled via persisted planned-id set; PendingIntent request codes = reminderNotificationId(eventId), stable across replans) + ReminderReceiver. (5) Wiring: TodayViewModel gains optional onLoaded hook (fires per projection emission), KompaktApplication installs replan behind a remoteActive guard (fake-mode projections never touch system alarms); AlertSyncWorker.schedule() at app onCreate. Manifest: SCHEDULE_EXACT_ALARM + receiver. Tests +22 (store 7, planner 7, sync-core 6 incl. Bearer/500-propagation, VM hook 2) — 182/182 green, lint clean. Lint debt note: T-019 shipped without a verify.sh pass and carried two lint errors (literal 0x40000000 → WrongConstant; NotificationManagerCompat.notify → MissingPermission) — fixed here (named ServiceInfo constant — compile-time inlined, runtime-identical; notify wrapped in SecurityException catch, which also covers TOCTOU revocation). Known gap: alarms don't survive device reboot (no BOOT_COMPLETED receiver of our own — WorkManager's is for its scheduling); next app open replans from the refreshed projection.
- [x] T-022a Phase 12 app slice — Notes surface (D028 v2; server V-060a/b already live): browse + edit all vault markdown from the phone, file-authoritative. Data: Note decodes wire rows directly (list rows omit text/checksum, detail adds both; role = scratchpad|inbox|note server-computed from folder); NoteRepository.updateNote carries the sha256 optimistic lock; HttpApi gains raw-JSON PUT + typed 409 — `detail.reason="checksum_mismatch"` with fresh note → NoteConflictException, any other 409 stays plain RepositoryException (agent busy-409 unaffected); Remote (idempotent create via requestId) + Fake (exercises checksum-mismatch) + AppContainer switching delegate. UI: NotesScreen pins the scratchpad as an "Unprocessed (N)" header, category sections (Inbox first), rows = displayTitle/listPreview/relative date, teaching empty state; NoteEditorScreen is read-first (MarkdownText + meta rows) → explicit Edit → TextFieldValue editor with mic insert-at-cursor, persistent "Saved HH:MM" meta line (no snackbar — e-ink), conflict banner clears only on Reload or successful save (user text never silently replaced on 409; fresh checksum adopted so second Save overwrites). insertAtSelection space-joins at the cursor (appendTranscript philosophy — voice inserts never run words together). Nav: NOTE_EDITOR route (18 screens), containerViewModel keyed "note-$id"; entry capability-gated server-side (notes feature already on — no client flag). Tests 210/210 (+17: VM 10 incl. both 409 contract paths, MockWebServer wire 5, insertAtSelection 3; fixture ripples: RoutesTest count → 18, capture envelope asserts displayTitle, unusedNoteRepo implements updateNote, Note.updatedAt now required). verify.sh green. On-device verify rides b-4.
- [x] T-022c Workspace picker on agent dispatch (server V-061/V-061b already merged): pick the git checkout a run starts in from the server-known registry instead of trusting free-text. Data: Workspace {ref, label} (@Serializable, refs opaque per D023) + WorkspaceRepository.observeWorkspaces() + RemoteWorkspaceRepository (GET /v1/workspaces via decodeList — cold one-shot like the agents surface); AgentBackendInfo gains @SerialName("workspace_selection") workspaceSelection=false (old servers omit → false, old clients ignore). Fake: FakeWorkspaceRepository over FakeData.workspaces demo (evershift/kompakt-interface/vault-coordinator/dev-server); opencode fake advertises the flag. AppContainer: RemoteStack.workspaces + stable SwitchWorkspace alongside the other switch delegates. VM: AgentDetailViewModel takes the workspace repo (3 test call sites updated) and exposes workspaces StateFlow. UI: dispatch screen renders the picker ONLY for backends advertising workspace_selection — collapsed ListRow ("Workspace: label|none") expanding to None + one row per workspace, ● trailing marks selection, no new material deps (e-ink ListRow primitives only); optional by design ("None" dispatches without a ref); Warren's required free-text project field unchanged and untouched by the picker path; capabilities panel + backend chip summary show the new flag. Tests 214/214 (+4: VM workspaces exposure + picked-ref lands in dispatch draft, MockWebServer wire contract incl. default-sibling tolerance + auth/path + empty registry). verify.sh green. On-device leg rides the phone plug-in (b-4 batch).
- [x] T-022d Chat scopes client leg (server V-062/V-063 already merged): three chat tiers — General (unscoped), Topic (vault-seeded, `00 - Inbox/*.md` seeds land in the first assistant reply), Workspace (repo-bound OpenCode + per-turn auto-commit). Data: ChatThread gains @SerialName scope_type/scope_ref/scope_label + proposedTopic/pendingReply (old servers omit → null/false); ChatTopic {id,label}; ChatThreadDraft carries scope params; ChatExchange gains proposedTopic. Repos: TopicRepository.observeWorkspaces-style observeTopics() (GET /v1/chat/topics) + RemoteTopicRepository (decodeList, cold one-shot); ChatRepository.setScope → POST /v1/chats/{id}/scope (nulls omitted from the body — clearing = absent keys); SendEnvelope extends proposed_topic/pending_reply. Fake: deterministic evershift proposal on unscoped send, labelFor mirrors server scope_label (topic by id, workspace by ref, raw-ref fallback), scope-aware create; demo topics + scoped thread so fake mode is indistinguishable. AppContainer: RemoteStack.topics + stable SwitchTopic + SwitchChat delegates setScope. VM: list — newChat(scopeType,scopeRef) + picker data; thread — proposedTopic StateFlow never auto-applied (applyProposal = explicit setScope("topic",id), dismissProposal is local-only), setScope(nulls) clears from the header picker, V-063 pending poll: 15 s re-tick loop while pending_reply. Poll design lesson: a collect-driven loop stalls after one tick — still-pending refetches re-emit an equal snapshot and the StateFlow dedupes it, so collect never re-fires (would also stall against the real server on unchanged pending JSON); fixed with a re-reading while-loop off thread.value, caught by the virtual-time test. UI: expandable new-chat picker (General/Topics/Workspaces, T-022c convention), thread header scope row + re-scope, pending-turn indicator, propose chip only on unscoped threads (Move/Not now), list rows carry scope label + preview. Tests 227/227 (+13: proposal lifecycle + stale-chip clear + scope-in-draft + pending re-poll/settled-never-poll virtual-time VM tests, topics/scope/send-envelope MockWebServer wire tests; ripple: AgentViewModelsTest unusedChatRepo implements setScope; poll tests share runTest's scheduler with Main via UnconfinedTestDispatcher(testScheduler)). verify.sh green (assembleDebug + testDebugUnitTest + lintDebug). On-device leg: Pixel connected — APK install + manual smoke against the live coordinator rides this merge.
- [x] T-022e Save-to-project + project context surface (server V-064 already merged): agent run detail's Note transition becomes a save-target picker — Inbox (default, no project_id) + vault projects only. Wire: note CreateBody gains @SerialName("project_id") passed from NoteDraft.projectId (explicitNulls=false → inbox saves omit the key entirely). VM: AgentRunDetailViewModel takes organizationRepository, exposes saveTargets StateFlow filtered to the vault:project: prefix (machine projects have no vault folder and V-064 422s — never offered), saveNote(project?) drafts NoteDraft(projectId, sourceType=agent_run, sourceId=run.id) with feedback naming the destination. New surface: ProjectsScreen rows open a project detail instead of jumping to tasks — Routes.PROJECT_DETAIL "organize/projects/{projectId}" (Routes.all 18→19, RoutesTest count updated) + ProjectDetailViewModel joining workspace-scoped chats client-side (companion workspaceRefFor: vault ids pass their server-slug suffix through, machine ids get the client slug mirror lowercase/spaces→hyphens — documented limitation: server-side slug overrides not replicated) and project notes via the existing GET /v1/notes?project_id= query; tasks stay one tap deeper (existing filter route). FakeData ids aligned to the real wire scheme (machine:project:evershift / vault:project:kodeverket / machine:project:kompakt-interface / machine:project:dev-server, task refs re-pointed) + demo project note + workspace-scoped thread so fake mode exercises the same joins as the server. Tests 236/236, 0 failed, 4 skipped (+9: MockWebServer wire — project_id present when targeted / absent for inbox + X-Request-Id header pin; VM — vault-only save targets, targeted draft + named feedback, inbox omits id; ProjectDetail — workspaceRefFor id families, workspace-chat join + note query, vault slug join, unknown id loads empty; ripple: AgentViewModelsTest vm() helper takes note/org repos). Stub lesson: observeProjects() is invoked EAGERLY when the VM builds the saveTargets chain — a throwing stub must throw on collection (flow{}), not on call, or every construction fails. verify.sh green (assembleDebug + testDebugUnitTest + lintDebug). On-device smoke rides the next plug-in alongside T-022d.
- [x] T-021 Phase 11 voice input (server V-059) — record → server STT → editable transcript in all four composers (capture, chat thread, agent dispatch objective, agent run-detail steering). voice/VoiceInput.kt: VoiceInputController (Idle→Recording→Uploading→Idle/Failed, taps only — explicit recording per §13), MediaRecorderVoiceRecorder (AAC/MP4, 16 kHz mono, 24 kbps — native av/whisper input), HttpVoiceTranscriber (multipart upload, all per-IO timeouts raised to 90 s — CPU whisper can take seconds; same taxonomy mapping), appendTranscript (empty draft → replace, else space-join). voice/VoiceUi.kt: rememberVoiceInput (returns null when not enrolled — mic hidden in fake mode; DisposableEffect → dispose cancels a live recording on screen-leave), MicButton (Icons.Filled.Mic/Stop from material-icons-extended — core pack lacks Mic; RECORD_AUDIO runtime prompt handled in-button: grant flows straight into recording; state-coded: square=stop, red=last-failure-retry, spinner=uploading), VoiceStatusText (failure reason line, renders nothing when healthy). HttpApi.postMultipart: shared multipart plumbing, same failure taxonomy as post. Manifest +RECORD_AUDIO. Server-side: voice.transcribe granted to pixel-4a-5g via admin re-approve (payload rebuilt from DB row — capabilities are per-request DB reads, no re-activation needed). Tests +11 (193/193 green, verify.sh pass): state machine (happy path, blank→"Nothing recognized", stop-throw→clip-too-short hint, offline/generic upload failures — both assert clip deletion (§13 no-retention), permission-denied→retry recovers, dispose cancels, gated transcriber proves mid-flight taps are no-ops), wire format vs MockWebServer (path/auth/multipart field names/audio mp4 part), append semantics. Testing lesson: controller coroutines on backgroundScope+advanceUntilIdle never ran the upload body (state stuck Uploading) — repo convention is UnconfinedTestDispatcher (eager); controller takes injectable ioDispatcher for exactly this. On-device leg: APK installed + RECORD_AUDIO pre-granted via pm grant; PIN lockscreen blocks adb-driven E2E — manual smoke: unlock → Capture → mic → speak Norwegian → stop → transcript lands as editable text (silent room should show "Nothing recognized — try again", which is itself the full-loop proof). Known scope cut: language hint not wired in UI (server auto-detects; "no"/"en" hint available on the endpoint for a later settings toggle).
- [x] T-022b Scratchpad section triage + chat save-note (server leg = zero changes, grounded against src/notes.py: `chat` already in SOURCE_TYPES, create_note_file targets already match the triage destinations). Domain: NoteSections parser (`## ` headings, em-dash-dated capture sections) + remove() deleting heading-through-next-## precisely. NoteEditorViewModel takes taskRepository + organizationRepository; read view on a sectioned scratchpad renders per-section triage cards instead of the editor: New note in… (Inbox + vault projects only — T-022e rule, machine ids would 422), Append to… (any non-scratchpad note except self; refetches target for a fresh checksum, appends heading+body verbatim), Create task (derive-only — section NOT consumed, TaskDraft sourceType=note), Keep (no-op), Discard (remove; git history is the undo). Crash-safety ordering oracle: every consuming action performs the external effect FIRST, source removal second — a crash between duplicates the section (safe direction), never loses it; target failure/conflict leaves the source byte-identical. All source PUTs checksum-guarded; conflict → triageNotice + auto-reload (reload(keepNotice=true) — reload previously rebuilt state wholesale and wiped the notice it had just set, caught by the ordering tests). Chat: per-assistant-message "Save as note" (expanded message actions) — verbatim text snapshot, source_type=chat/source_id=threadId, user messages never saveable (object-boundary rule: transitions are explicit taps, nothing auto-converts). Capture badge copy → "Saved to scratchpad: {title}". FakeData scratchpad demo doc (two dated sections) so fake mode exercises the same triage UI. Tests 253/253, 0 failed, 4 skipped (+17: 8 parser — heading/section/remove edges; 7 VM triage with a call-order recording fake — create-then-update, append-target-first, target conflict keeps source, discard, derive-keeps-section, source-conflict reloads w/ notice; 2 chat saveNote — provenance + failure notice, user-message rejection). Test-fake lessons: updateNote stubs must route by note id (a store-only fake conflicts every target PUT with the scratchpad's checksum), and a concurrent-write race needs a staleFetchFor knob (getNote hands out a checksum storage has moved past) — the VM always refetches, so a hand-built "stale note" object can never produce a target conflict. ChatViewModelsTest: each test initializes the class lateinit repo itself — new tests forgot (UninitializedPropertyAccessException at construction). verify.sh green (assembleDebug + testDebugUnitTest + lintDebug). On-device smoke rides the next plug-in batch (with T-022d/T-022e).
- [x] T-023 Calendar surface (server V-065 already merged): month grid + day agenda, event detail/editor, capture→event. Domain: Calendar.kt — CalendarInfo (display_name/symbol/writable, omitted writable ⇒ read-only SA), EventDraft (POST body incl. request_id), EventUpdate (PATCH, no all_day per wire), EventCreateResult(id,status, replayed="already_exists"), MonthGrid (42-cell Monday-first grid via dayOfWeek.ordinal — kotlinx's enum has no .value; window = grid span so lead/trail days render events; byDay groups by Europe/Oslo date). CalendarEvent (Today.kt) gains calendar_id/symbol/description/all_day — defaults keep old wire decodable. Repos: CalendarRepository (observeCalendars, fetchWindow GET /v1/schedule/range?from&to, fetchEvent GET /v1/events/{id} — 404→null, occurrence ids ~slot pass through, create/update/delete); RemoteCalendarRepository (HttpApi gains patch()/delete(); POST /v1/events carries request_id in body; DELETE returns 200+body → true); FakeCalendarRepository stateful (id {calendar}:{uid} from request-id hash, idempotent replay, ~ occurrences as seeded entries, no recurrence expansion — documented). UI: CalendarScreen (42-cell monochrome grid, today/selected borders, per-day symbol dots, day agenda below), EventDetailScreen, EventEditorScreen (naive date/time input parsed in Europe/Oslo → UTC Z wire; all-day sends no end; +1h default end; writable gating — read-only registries rejected before wire; occurrence banner). Routes 19→22 (CALENDAR, EVENT_DETAIL, EVENT_EDITOR?date prefill), More row "Calendar". Capture: CaptureType.EVENT + proposal start/end/all_day/calendar_id; events BYPASS /v1/capture/commit (server 501s kind=event) — CaptureViewModel.commitEvent POSTs /v1/events directly with the same request id (idempotent), describes via EventCreated; AppContainer hoists ONE shared FakeCalendarRepository so demo calendar + capture commits observe the same state; FakeCapture interpret mirrors coordinator _EVENT_PREFIX (avtale/møte/event/meeting → EVENT with a fake start so quick-confirm works). Dependency bumps forced by datetime needs: kotlinx-datetime 0.6.1→0.7.1 (YearMonth shipped 0.7; 0.7 REMOVED InstantIso8601Serializer and Clock → kotlin.time), Kotlin 2.0.21→2.1.21 (0.7.x metadata needs ≥2.1.20 for kotlin.time.Clock; 2.1.10 broke serialization-plugin on untouched files), global -opt-in=kotlin.time.ExperimentalTime, project-local domain.InstantIso8601Serializer drop-in (same package as all consumers — no import swaps beyond deleting the kotlinx one), Clock imports → kotlin.time.Clock (14 files), TimeZone("x")→TimeZone.of("x"), minSdk 28 makes the java.time typealias safe. Tests 273/273, 0 failed, 4 skipped (+20: 8 wire vs MockWebServer — calendars display_name/default read-only, range query+event decode incl. ~ids, POST body request_id/calendar_id/start_at, replay status, PATCH method+path, DELETE, single fetch, 404→null; 12 VM — month nav + selected-day clamp, seeded events on Oslo day, create/delete outcome, editor defaults (+1h), prefill from fetch, naive-Oslo→UTC math (09:00 CEST=07:00Z), all-day no-end, end-before-start local reject, read-only reject, edit PATCH, capture→event commit via shared fake + no-start rejection; RoutesTest 22). Test lessons: EventViewModel.calendars is stateIn(WhileSubscribed) — save() gating reads .value, tests must backgroundScope-subscribe + runCurrent() first or every create fails "Unknown calendar"; body assertions on MockWebServer must not whitespace-strip (destroys spaces inside values — assert field-level substrings); FakeData seeds a "Dentist" event — unique fixture titles or first{} grabs the seed. TASKS.md stale-writer recurred mid-task (Backlog header eaten again) — restored via git checkout before ledger write. verify.sh green (assembleDebug + testDebugUnitTest + lintDebug, 2m39s). On-device smoke via wireless ADB (debug on) pending plug-in.
- [x] T-023b Calendar entry fixes (on-device smoke follow-up, three findings): (1) Create 403 "capability 'calendar.write' required" — server-side root cause: pixel-4a-5g had calendar.read but not calendar.write (V-065b two-layer authz is device capability × registry writable; the phone was never re-granted after V-065 shipped). Fixed via scripts/devices.py approve dev-5f9d854ed542 with the FULL rebuilt capability list (14→15 caps; approve without --capabilities RESETS to default reads — same trap as the voice.transcribe grant). Capabilities are per-request DB reads → effective immediately, no coordinator restart. (2) Calendar Today button — header previously only had < >; added OutlinedButtonMMD "Today" before them → CalendarViewModel.goToToday() (month = today.yearMonth, selectedDay = today, loadWindow). VM init now selects today on open — fixes the smoke finding that the day agenda was invisible until a tap, and matches the plan's "enters at today, selected". (3) Today → Calendar entry — the T-023a plan's date-header tap (D030 entry point) never shipped; first cut shipped as a tappable date-header row, then user-redirected same day to an icon-only IconButton (Icons.Filled.CalendarMonth, icons-extended) in the Today app bar next to the notification bell — hidden, minimal real estate; onOpenCalendar defaults {} so existing callers/tests compile; wired in KompaktApp. Tests +1 → 274 (today-initial + goToToday restore; nav-clamp test unaffected — it selects explicitly). Stale-writer strike #12 at session start (T-023 ledger entry + Backlog header eaten in the worktree, HEAD intact) — restored via git checkout before edits. verify.sh green (1m07s). On-device: APK installed over wireless adb; manual 3-tap check is the user leg (phone PIN-locked for adb): header row → Calendar, Today button after month-paging, retry the previously-403 Create.

## Backlog (unplanned / revisit)

- [x] T-026 Today redesign — three-tab surface (NOW / TASKS / ATTENTION) shipped per plan `docs/plans/2026-08-28-t026-today-redesign.md` (D031). ViewModel (TodayViewModel.kt rewritten): state pipeline combine(refreshTick.flatMapLatest{observeToday+hook+catch}, nextBeyondToday, notice) — hero fall-through join (remaining-today first, else fetchWindow(now..+7d Oslo) min-by startAt, display-only, failure degrades silently to null per T-024; re-fetch guarded by value!=null so the combine re-emission can't loop); quick-complete = the ONE D031 mutation (completeTask(id, revision, fresh requestId) → success clears notice / RevisionConflict → "Changed on server — showing latest" / else userMessage; every outcome bumps refreshTick which re-collects the COLD observeToday flow = T-012 refresh pattern); splits: pastEvents/remainingEvents partition at now (sorted), openTasks by status, doneToday by status+updatedAt-in-Oslo-today. Constructor grew calendarRepository/taskRepository/newRequestId AFTER existing params (defaults) — HookTest call sites unchanged. Screen (TodayScreen.kt rebuilt): title "Today · Fri 28 Aug" (fixed-English, Oslo), static-text tab rail (bold + rule-underline selected, inline counts TASKS n / ATTENTION n when >0, no moving badges) + HorizontalPager (foundation, zero new deps), rail taps scrollToPage (instant — no animation, e-ink); NOW = hero CardMMD (NEXT UP / title / "15:00 · in 1h" via new Format.untilLabel / location) + Later rows + Earlier rows (secondary weight) + "· now ·" marker + Anytime band (untimed = dueAt null only) — a view, no mutations; TASKS = notice line + Due-today whole-row-tap complete (○→✓) + Done today (secondary, not tappable — un-complete is NOT granted by D031) + quiet-day single line; ATTENTION = one bordered ListRow per item, T-018 deep-link behavior preserved verbatim; empty sections render nothing. Cuts: Agents + Recent-note sections gone from Today (agent results still surface as attention cards; placement reopened — user Aug 28); SurfaceGating loses agentsSectionOnToday/recentNoteOnToday fields + assignments; KompaktApp call site + 6 SurfaceGatingTest assertions updated. Common.ListRow gains secondary:Boolean (de-emphasis by font weight only) — placed BEFORE onClick so trailing-lambda call sites (Chat/Note screens) still bind to onClick (compile-order trap hit and fixed). Format.untilLabel = day-first bucketing (same local day → in Xm/in Xh; +1 day → "tomorrow 09:00"; else date) — first cut bucketed by raw seconds and showed "in 21h" for tomorrow 09:00, test caught it. UX doc Today section rewritten (tabs diagram + rules + "empty sections render nothing"). Tests 298/298, 0 failures (+10: 9 VM — beyond-today join w/ Oslo window strings, calendar-failure degrade, remaining-wins-no-fetch, null-repo tolerance, complete happy path id/revision/requestId, conflict notice, generic-failure notice, task split, event split; 1 FormatTest untilLabel buckets). verify.sh green (assembleDebug + testDebugUnitTest + lintDebug, 1m01s). Stale-writer strike #17 mid-session (TASKS.md −13 lines, pure deletions, restored via git checkout before ledger write). On-device smoke pending next plug-in (hero fall-through against live Radicale calendar, quick-complete against live tasks, swipe feel per refresh-rate).

- [x] T-027 Today tab-rail fix (found in on-device smoke Aug 29, first T-026 device run): only "NOW" visible + rail never updates — TodayTabLabel's underline `Box(fillMaxWidth())` inside unweighted Row children lets the FIRST tab consume the whole row width (TASKS/ATTENTION starve to 0px, culled from a11y tree; centered MMD text default made it look like a single centered header). Fix: `Column(Modifier.width(IntrinsicSize.Min))` so labels hug text width; branch `fix/T-027-today-tab-rail`. On-device Aug 29: all three labels render with real bounds (NOW/TASKS/ATTENTION n incl. count badges); tap-switch check verified Aug 29 on T-029 build (TASKS/ATTENTION pages switch on rail taps). Also found in same smoke: server URL must be tailscale HOSTNAME (SNI — bare-IP TLS fails), fresh install has no enrollment nudge (fake-mode empty timeline reads as real data). Stale-writer strike #18 (Backlog header + T-024/T-025 sub-bullets eaten in worktree; restored via git checkout before this write).
- [x] T-028 Today page scroll (found in on-device audit Aug 29, same smoke as T-027): pager pages CLIP overflowing content — no scroll possible. Cause: AppScreen wraps content in a verticalScroll column, but HorizontalPager fills the viewport → outer scroll has zero range, and each page's plain Column clips at page bounds. On-device: ATTENTION with 7 items showed ~2 cards + one cut mid-card (vision-confirmed); items 3-7 unreachable. Same trap awaits TASKS on a busy day and NOW with a long timeline. Fix (two iterations): (1) per-page scrollable Column — CRASHED on launch ("scrollable component measured with an infinity maximum height": a scrollable scaffold hands the pager unbounded height, so scrollable page children are illegal); (2) final: AppScreen gains `scrollable: Boolean = true` (Today passes false) + per-page `verticalScroll` columns — bounded height reaches pages, pages own vertical scroll, horizontal drags still go to the pager. On-device verified: 14 synthetic time-blocks events, swipe scrolled ~1000px, previously unreachable rows reachable; events deleted after. Branch `fix/T-028-today-page-scroll`. Audit context: everything else on-device matched server truth 1:1 (calendar dots, agents, more); the rest of the "feels wrong" was junk data — 7 test inbox alerts (6 marked read via POST /v1/inbox/{id}/read; "laptop unreachable" is live-computed from machines registry, not stored), 3 test events Aug 12 deleted via DELETE /v1/events/{id}, 15 E2E chat threads + 36 messages purged direct-SQL (chat_threads/chat_messages; no API delete exists, truncate keeps shells). Stale-writer strikes #18 AND #19 mid-task (same Backlog header + T-024/T-025 sub-bullets eaten twice; both restored via git checkout).
- [x] T-029 Tab rail spread + single-line labels (user on-device report Aug 29, follow-up to T-027): tabs packed left instead of distributing, and "ATTENTION 1" wrapped its count onto a second row. Two causes, both mine: (1) T-027's IntrinsicSize.Min hugs the widest WORD — multi-word text wraps inside it, and unweighted labels don't distribute; (2) "ATTENTION n" at default font exceeds a 1/3 slot even unwrapped. Fix: TodayTabLabel Column weight(1f) (equal thirds; the fillMaxWidth underline is safe in a weighted slot — this ALSO fully fixes T-027's starvation, IntrinsicSize approach retired, imports dropped incl. now-unused Arrangement) + fontSize 13.sp (MMD legible small; 12.sp precedent NoteEditor meta). On-device verified: all three labels h=44 single-line, text centers 205/540/875 ≈ slot centers, headroom for 2-digit counts. Branch fix/T-029-tab-rail-spread. Stale-writer strike #20 pre-commit (same 13 deletions; restored).
- [ ] Revisit: liveSteering surface (Steer button + steer wire path) — user reads it as bloat (Aug 24). Cost today ≈ zero visible (renders only for liveSteering backends; current daily use is OpenCode, which never shows it). Decision rule: if no Warren steering use by the next feature pass, strip UI + repository method to the send-only path.
- [ ] Device button mapping smoke (rescoped Aug 28, supersedes "calendar dpad nav" — premise was WRONG: the Mudita Kompakt has NO physical keyboard, only side buttons with unknown function, per user). Old Aug 26 finding ("arrow keys do nothing on the month grid") came from adb-injected keyevents — a non-issue on keyboardless hardware. New scope: with device on ADB, `adb shell getevent -l`, press every physical button, record keycodes; check whether any leak into app views (e.g. volume keys during Calendar do nothing useful). Outcome decides if button-nav work is warranted — only plausible candidate: volume rocker as Calendar month prev/next.
- [x] T-024 Client capability negotiation + graceful degradation (server leg pairs with V-066): /v1/capabilities returns the device's OWN granted caps; the app resolves its cap set at stack build and surfaces lacking their read cap hide/soft-disable with a "needs re-approval" hint instead of error screens. Fan-out VMs (ItemDetail 5-flow combine, ProjectsViewModel, AreasViewModel) degrade per-flow — one failing source (403/404/5xx) renders missing data, never collapses the whole screen. Root cause (Aug 27 incident): pixel-4a-5g approved without `project.read` → GET /v1/projects 403 → task detail / Projects / Areas all landed in error state (the tasks list itself was fine — 200s in the access log); combine→catch maps ANY single flow failure to a whole-screen error.
  - Done Aug 28 2026, paired with server V-069 (vault-coordinator `94e448a`: /v1/capabilities serves the caller's own granted set to device principals; admin callers keep the old shape — no leak, verified live).
  - CapabilitySet decodes the V-069 `granted` block (`GrantedCapabilities` device_id + capabilities); absent ⇒ null = grants unknown → `grants()` fails OPEN to feature flags alone — pre-V-069 servers, admin contexts and demo mode never brick on capability hiding.
  - SurfaceGating: every surface now requires feature flag AND (when grants are known) its read grant — new CAP_CHAT_READ/"chat.read", CAP_AGENT_READ, CAP_NOTE_READ, CAP_INBOX_READ, CAP_CAPTURE_INTERPRET consts. The Aug-27 shape (flag on, grant missing) hides the surface instead of erroring on open.
  - Taxonomy: HttpApi 403 → ForbiddenException(capability), parsed from require_capability bodies (`capability 'x' required`); any other 403 keeps capability=null. userMessage: "Missing permission (x) — re-approve device in Settings" (generic variant without a name).
  - Degrade (display-only joins only — primary flows keep honest errors): TasksViewModel titleFlow ByProject/ByArea .catch→null (null title renders the grouped layout; the repo-level filter still scopes the set); ProjectsViewModel open-count join .catch→empty; ItemDetail typed-leg project join + every fallback-leg probe independently .catch→null/empty. AreasViewModel has NO join (single observeAreas flow) — its 403 legitimately errors, now with the named-capability line.
  - Tests +9 → 42 suites / 288 tests / 0 failures / 4 skipped: SurfaceGatingTest +3 (grant-missing hides surface, null granted fails open, full grant set shows all); RemoteContractTest +3 (403 named capability, 403 other-detail → null, V-069 granted block decode); TasksViewModelTest +2 (filter title degrades + task survives in grouped layout, open counts degrade to 0); ItemDetailViewModelTest +1 (fallback survives 3 simultaneously failing probes — quiet not-found, no crash); FormatTest +2 userMessage legs. Test lessons: the title join reads observeProject(id) SINGULAR — a failing-repo wrapper overriding only observeProjects() never exercises the degrade; title=null flips TasksViewModel to the grouped layout by design (assert today+upcoming, not open).
  - verify.sh green (assembleDebug + testDebugUnitTest + lintDebug, 1m08s). Stale-writer strike #14 (Backlog header AND T-025's done sub-bullets eaten in the worktree) — both restored before this commit.
- [x] T-025 Kind-aware item detail navigation: Routes.ITEM_DETAIL carries the entity kind from the originating list (task rows → task; inbox keeps the T-018 resolver knowledge); ItemDetailViewModel fetches the ONE typed repository instead of the 5-flow combine. Eliminates the fan-out fragility class (T-024's other half) AND the /v1/notes/<task-ref> probe noise (3× 404 per task-detail open, Aug 27 logs — getNote swallows 404→null, so the requests are pure waste). Unknown-kind deep links keep the generic fan-out as fallback.
  - Done Aug 28 2026. Route `item/{itemId}?kind={kind}` (null kind = bare route, unchanged); `Routes.item(id, kind)` + `Routes.itemKind(wire)` (junk + `unknown` sentinel degrade to null → fallback). TasksScreen wiring passes `EntityKind.TASK`. Typed leg: `observeTask` + `observeProjects().catch{emptyList()}` — the project join is display-only and degrades to unnamed instead of killing the screen (Aug-27 incident class). kind=null keeps the 5-flow combine verbatim.
  - Tests: ItemDetailViewModelTest 4 checks — typed leg renders + recording repos prove observeNote/observeRun/observeInbox NEVER called (probe-noise kill), typed leg survives projects-join failure, unknown kind still uses fallback (inbox leg), typed not-found. RoutesTest +2 legs (builder w/ kind, itemKind parsing incl. sentinel). Full suite: 42 suites, 279 tests, 0 failures; verify.sh OK (1m27s).

